# نفاذ (Nafath) Integration Configuration
# These environment variables are required for نفاذ OAuth integration

# نفاذ Base URL (subdomain assigned by Saudi Digital Government)
NAFATH_URL_BASE=your-subdomain.nafath.sa

# OAuth Client Credentials (obtained from نفاذ registration)
NAFATH_CLIENT_ID=your-client-id
NAFATH_CLIENT_SECRET=your-client-secret

# Application URL (for OAuth callback)
APP_URL=http://localhost:5000

# Instructions:
# 1. Copy this file to .env.nafath
# 2. Fill in the actual values obtained from نفاذ registration
# 3. Restart the application to enable نفاذ integration
#
# Note: Without these credentials, نفاذ integration will be disabled
# and users will see only manual form entry options.